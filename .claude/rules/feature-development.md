# feature-development

- **Branch:** `feature/<slug>` branched from `master`.
- **Test collaboration mode:** Assert-in-the-loop — scaffold one test (arrange + act +
  empty assertion), the human writes the assertions, then confirm genuine red and
  implement to green.
- **Review hand-off:** delegate to the `code-review` skill.
