#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# no --ensure-latest: a new upstream release is not a reason to fail the build.
# Dependabot keeps the gem current; this binstub only reports real findings.
load Gem.bin_path("brakeman", "brakeman")
